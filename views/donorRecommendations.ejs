<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Donor Recommendations - Blood Bank System</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-red-50 via-pink-50 to-red-100 min-h-screen">
  
  <!-- Navigation -->
  <nav class="bg-white/90 backdrop-blur-md shadow-lg sticky top-0 z-40 border-b border-red-100">
    <div class="container mx-auto px-6 py-4">
      <div class="flex items-center justify-between">
        <div class="flex items-center space-x-3">
          <h1 class="text-2xl font-bold text-red-600">Donor Recommendations</h1>
        </div>
        <div class="flex items-center space-x-4">
          <a href="/hospital-dashboard" class="text-gray-700 hover:text-red-600 font-medium transition-colors">
            Back to Dashboard
          </a>
          <span class="text-sm text-gray-600"><%= (user && user.email) || 'User' %></span>
        </div>
      </div>
    </div>
  </nav>

  <!-- Main Content -->
  <div class="container mx-auto px-6 py-8">
    
    <!-- Request Details Card -->
    <div class="bg-white rounded-2xl shadow-xl p-6 mb-8">
      <h2 class="text-2xl font-bold text-gray-800 mb-4">Request Details</h2>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div>
          <p class="text-sm text-gray-600">Request ID</p>
          <p class="font-semibold text-gray-900">#<%= request._id.toString().slice(-8) %></p>
        </div>
        <div>
          <p class="text-sm text-gray-600">Blood Group</p>
          <p class="font-semibold text-red-600 text-lg"><%= request.bloodGroup %></p>
        </div>
        <div>
          <p class="text-sm text-gray-600">Patient Name</p>
          <p class="font-semibold text-gray-900"><%= request.patientName %></p>
        </div>
        <div>
          <p class="text-sm text-gray-600">Units Required</p>
          <p class="font-semibold text-gray-900"><%= request.unitsRequired %></p>
        </div>
        <div>
          <p class="text-sm text-gray-600">Urgency</p>
          <span class="px-2 py-1 text-xs font-semibold rounded-full 
            <%= request.urgency === 'critical' ? 'bg-red-100 text-red-800' : 
                request.urgency === 'high' ? 'bg-orange-100 text-orange-800' : 
                request.urgency === 'medium' ? 'bg-yellow-100 text-yellow-800' : 
                'bg-green-100 text-green-800' %>">
            <%= request.urgency %>
          </span>
        </div>
        <div>
          <p class="text-sm text-gray-600">Status</p>
          <span class="px-2 py-1 text-xs font-semibold rounded-full 
            <%= request.status === 'fulfilled' ? 'bg-blue-100 text-blue-800' : 
                request.status === 'approved' ? 'bg-green-100 text-green-800' : 
                request.status === 'rejected' ? 'bg-red-100 text-red-800' : 
                'bg-yellow-100 text-yellow-800' %>">
            <%= request.status %>
          </span>
        </div>
      </div>
      
      <% if (prediction && prediction !== 'No prediction available') { %>
      <div class="mt-6 p-4 bg-blue-50 border-l-4 border-blue-500 rounded">
        <p class="text-sm text-gray-600 mb-1">🧠 ML Prediction</p>
        <p class="font-semibold text-blue-700"><%= prediction %></p>
      </div>
      <% } %>
    </div>

    <!-- Recommended Donors -->
    <div class="bg-white rounded-2xl shadow-xl p-6">
      <h2 class="text-2xl font-bold text-gray-800 mb-6 flex items-center">
        <svg class="w-6 h-6 mr-2 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z"></path>
        </svg>
        Top Recommended Donors
      </h2>

      <% if (donors && donors.length > 0) { %>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          <% donors.forEach((donor, index) => { %>
            <div class="bg-gradient-to-br from-gray-50 to-white border-2 border-gray-200 rounded-xl p-6 hover:shadow-lg transition-all duration-200">
              <div class="flex items-start justify-between mb-4">
                <div class="bg-red-100 rounded-full w-12 h-12 flex items-center justify-center">
                  <span class="text-red-600 font-bold text-lg">#<%= index + 1 %></span>
                </div>
                <span class="px-3 py-1 bg-red-600 text-white text-xs font-semibold rounded-full">
                  <%= donor.bloodGroup %>
                </span>
              </div>
              
              <h3 class="text-lg font-bold text-gray-900 mb-3"><%= donor.fullName || 'Anonymous Donor' %></h3>
              
              <div class="space-y-2 text-sm">
                <div class="flex items-center text-gray-600">
                  <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path>
                  </svg>
                  <%= donor.phone || 'N/A' %>
                </div>
                
                <% if (donor.address) { %>
                <div class="flex items-center text-gray-600">
                  <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                  </svg>
                  <%= donor.address.city %>, <%= donor.address.state %>
                </div>
                <% } %>
                
                <% if (donor.donationCount !== undefined) { %>
                <div class="flex items-center text-gray-600">
                  <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4M7.835 4.697a3.42 3.42 0 001.946-.806 3.42 3.42 0 014.438 0 3.42 3.42 0 001.946.806 3.42 3.42 0 013.138 3.138 3.42 3.42 0 00.806 1.946 3.42 3.42 0 010 4.438 3.42 3.42 0 00-.806 1.946 3.42 3.42 0 01-3.138 3.138 3.42 3.42 0 00-1.946.806 3.42 3.42 0 01-4.438 0 3.42 3.42 0 00-1.946-.806 3.42 3.42 0 01-3.138-3.138 3.42 3.42 0 00-.806-1.946 3.42 3.42 0 010-4.438 3.42 3.42 0 00.806-1.946 3.42 3.42 0 013.138-3.138z"></path>
                  </svg>
                  <%= donor.donationCount %> donations
                </div>
                <% } %>
                
                <% if (donor.lastDonationDate) { %>
                <div class="flex items-center text-gray-600">
                  <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                  </svg>
                  Last: <%= new Date(donor.lastDonationDate).toLocaleDateString() %>
                </div>
                <% } %>
              </div>
              
              <a href="tel:<%= donor.phone %>" class="mt-4 block w-full text-center bg-red-600 hover:bg-red-700 text-white font-semibold py-2 px-4 rounded-lg transition-colors">
                Contact Donor
              </a>
            </div>
          <% }); %>
        </div>
      <% } else { %>
        <div class="text-center py-12">
          <svg class="w-16 h-16 mx-auto text-gray-400 mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z"></path>
          </svg>
          <p class="text-gray-600 text-lg">No matching donors found nearby.</p>
          <p class="text-gray-500 text-sm mt-2">Try expanding your search area or contact blood banks directly.</p>
        </div>
      <% } %>
    </div>

    <!-- Action Buttons -->
    <div class="mt-8 flex justify-center space-x-4">
      <a href="/hospital-dashboard" class="px-6 py-3 bg-gray-600 hover:bg-gray-700 text-white font-semibold rounded-lg transition-colors">
        Back to Dashboard
      </a>
      <a href="/bloodBank/allBanks" class="px-6 py-3 bg-red-600 hover:bg-red-700 text-white font-semibold rounded-lg transition-colors">
        Find Blood Banks
      </a>
    </div>
  </div>

</body>
</html>